baseURL = "https://sysarmy.com/blog/"
languageCode = "en-us"
theme = "hello-friend"
publishDir = "/"
MetaDataFormat = "toml"

[pagination]
  # Sets 10 items per page
  pagerSize = 10

[params]
  # Site description and default image for Open Graph / Twitter cards (social embeds)
  # Hero image is used when your blog URL is shared (Twitter, WhatsApp, etc.). Put the file in static/images/
  # For link previews use image 1200×630px (1.91:1), ideally under 300KB
  description = "El blog de quienes dan soporte - Sysarmy"
  heroImage = "images/og-image.png"
  heroImageWidth = 1200
  heroImageHeight = 630
  favicon = "images/og-image.png"

  # dir name of your blog content (default is `content/posts`).
  # the list of set content will show up on your index page (baseurl).
  contentTypeName = "posts"

  # "light" or "dark"
  defaultTheme = "dark"

  # if you set this to 0, only submenu trigger will be visible
  showMenuItems = 2

  # Show reading time in minutes for posts
  showReadingTime = true

  # Show table of contents at the top of your posts (defaults to false)
  # Alternatively, add this param to post front matter for specific posts
  # toc = true

  # Show full page content in RSS feed items
  #(default is Description or Summary metadata in the front matter)
  # rssFullText = true

[languages]
  [languages.en]
    title = "Sysarmy"
  [languages.en.params]
    subtitle = "El blog de quienes dan soporte"
    keywords = "sysadmin, sistemas, desarrollo, developer, administración de sistemas, administrador de sistemas, linux, cloud, docker, kubernetes"
    copyright = ""
    menuMore = "Mostrar mas"
    writtenBy = "Escrito por"
    readMore = "Seguir leyendo"
    readOtherPosts = "Leer otros posts"
    newerPosts = "Nuevos posts"
    olderPosts = "Posts anteriores"
    minuteReadingTime = "minutos de lectura aprox."
    dateFormatSingle = "January 2, 2006"
    dateFormatList = "January 2, 2006"
    # leave empty to disable, enter display text to enable
    # lastModDisplay = ""

    [languages.en.params.logo]
      logoText = "sysarmy:~ blog$"
      logoHomeLink = "/blog"
    # or
      #path = "/blog/assets/logo.png"
      #alt = "Sysarmy"

    [languages.en.menu]
      [[languages.en.menu.main]]
        identifier = "Youtube"
        name = "Youtube"
        url = "https://www.youtube.com/channel/UCPE3EUzO58EBHzrJp2Fv7_A"
      [[languages.en.menu.main]]
        identifier = "Tienda"
        name = "Tienda"
        url = "https://tienda.sysarmy.com/"

footnoteReturnLinkContents = ":leftwards_arrow_with_hook:"

[author]
    name = "Sysarmy"
